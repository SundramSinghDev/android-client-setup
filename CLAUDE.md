# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Purpose

This project automates the creation of new client projects — scaffolding, configuration, and any other setup work that currently happens manually when onboarding a new client.

## Current State

The repository is empty. Architecture, tooling, and language choices are yet to be decided.

When those decisions are made, update this file with:
- Build, lint, and test commands
- How the automation pipeline is triggered (CLI, API, webhook, etc.)
- What a "project creation" produces (folder structure, configs, repos, CI/CD, etc.)
- Any external services or credentials required (e.g. GitHub API, Jira, cloud providers)
